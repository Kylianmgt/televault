requests>=2.28
fastapi>=0.100
uvicorn[standard]>=0.20
Pillow>=9.0
wsgidav>=4.0
cheroot>=9.0
# Required by /api/upload: FastAPI raises at import time on any UploadFile
# route without it, so the server cannot start at all when it is missing.
python-multipart>=0.0.6
